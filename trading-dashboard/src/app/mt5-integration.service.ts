import { Injectable } from '@angular/core';\nimport { HttpClient } from '@angular/common/http';\nimport { firstValueFrom } from 'rxjs';\n\ninterface Table {\n  openDate: string;\n  tradeNotion: any[];\n  status: string;\n  position: string;\n  symbol: string;\n  type: string;\n  volume: string;\n  entry: string;\n  sL: string;\n  tP: string;\n  closeDate: string;\n  exit: string;\n  commission: string;\n  swap: string;\n  profit: string;\n  netProfit: string;\n}\n\n@Injectable({\n  providedIn: 'root'\n})\nexport class Mt5IntegrationService {\n\n  constructor(private http: HttpClient) { }\n\n  async getMt5API(): Promise<any> {\n    const res: any = await firstValueFrom(\n      this.http.get(\"http://localhost:5000/api/open_trades\")\n    );\n    console.warn(\"Getting MT5 API data...\", res);\n    return res;\n  }\n\n  // Convert MT5 trade data to Table interface\n  convertMT5ToTable(mt5Trade: any, isClosed: boolean = false): Table {\n    const openDate = new Date(mt5Trade.time * 1000).toLocaleString('en-US', {\n      month: '2-digit',\n      day: '2-digit', \n      year: 'numeric',\n      hour: '2-digit',\n      minute: '2-digit'\n    }).replace(/\\//g, '.').replace(', ', ' ');\n\n    const closeDate = isClosed && mt5Trade.time_close ? \n      new Date(mt5Trade.time_close * 1000).toLocaleString('en-US', {\n        month: '2-digit',\n        day: '2-digit',\n        year: 'numeric', \n        hour: '2-digit',\n        minute: '2-digit'\n      }).replace(/\\//g, '.').replace(', ', ' ') : '';\n\n    return {\n      openDate: openDate,\n      tradeNotion: [],\n      status: isClosed ? 'Closed' : 'Open',\n      position: mt5Trade.type === 0 ? 'Buy' : 'Sell',\n      symbol: mt5Trade.symbol || '',\n      type: mt5Trade.type === 0 ? 'Buy' : 'Sell',\n      volume: mt5Trade.volume ? mt5Trade.volume.toString() : '0',\n      entry: mt5Trade.price_open ? mt5Trade.price_open.toString() : '0',\n      sL: mt5Trade.sl ? mt5Trade.sl.toString() : '0',\n      tP: mt5Trade.tp ? mt5Trade.tp.toString() : '0',\n      closeDate: closeDate,\n      exit: isClosed && mt5Trade.price_close ? mt5Trade.price_close.toString() : mt5Trade.price_current ? mt5Trade.price_current.toString() : '0',\n      commission: '0', // MT5 might not provide this directly\n      swap: mt5Trade.swap ? mt5Trade.swap.toString() : '0',\n      profit: mt5Trade.profit ? mt5Trade.profit.toString() : '0',\n      netProfit: mt5Trade.profit ? mt5Trade.profit.toString() : '0'\n    };\n  }\n}\n